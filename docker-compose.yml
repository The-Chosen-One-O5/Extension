version: '3.8'

services:
  bot:
    build: .
    container_name: telegram-voice-bot
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - ./temp_audio:/app/temp_audio
